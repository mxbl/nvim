vim.diagnostic.config {
    underline = true,
    signs = true,
    float = {
        show_header = true,
        -- format = function(d)
        --     return "test"
        -- end,
    },
    update_in_insert = false,
}
